<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huangtl.blogmgr.model.blog.LoginLog">

	<resultMap id="mapper" type="com.huangtl.blogmgr.model.blog.LoginLog" >
		<id property="fId" column="login_id"/>
		<result property="fUid" column="ui_id"/>
		<result property="fIp" column="login_ip" />
		<result property="fBrowser" column="login_browser"/>
		<result property="fCreateDate" column="login_create_date"/>
		<result property="fOperatingSystem" column="login_operating_system"/>
		
		<result property="userName" column="ui_name"/>
		<result property="userAccount" column="ui_account"/>
	</resultMap>
	
	<sql id="field">
		login_id ,ui_id ,login_ip,login_browser,login_create_date as fCreateDate,
		ui_name,ui_account,login_operating_system
	</sql>
	
	<sql id="where">
		 <trim prefix="where" prefixOverrides="and|or">
		 	<if test="defaultWhere!=null and defaultWhere">
		 		1=1
		 	</if>
		</trim>
	</sql>
	
	<sql id="order">
		<if test="sorts != null and sorts.size() != 0">
			order by 
			<foreach collection="sorts" separator="," index="key" item="val">
				${key} ${val}
			</foreach>
		</if>
	</sql>
	
	<insert id="insert" parameterType="com.huangtl.blogmgr.model.blog.Role">
		insert into sys_LoginLog(
			login_id,ui_id,login_ip,login_browser,login_create_date,login_operating_system
		) values(
			#{fId},#{fUid},#{fIp},#{fBrowser},#{fCreateDate},#{fOperatingSystem}
		);
	</insert>
	
	<select id="selectList" resultMap="mapper" parameterType="map">
		select 
			<include refid="field"/>
		from sys_LoginLog
			<include refid="where"/>
	</select>
	
	<select id="selectCount"  resultType="long" parameterType="map">
	  select count(login_id)
	  from sys_LoginLog
		<include refid="where"/>
	</select>
	
	<select id="selectPaging" resultMap="mapper" parameterType="map">
		select 
			<include refid="field"/>
		from sys_LoginLog a
		left join (
			select ui_Id as uid, ui_Name, ui_Account  from mgr_userinfo
		)b on a.ui_id = b.uid
			<include refid="where"/>
			<include refid="order"/>
		limit #{startIndex},#{pageSize}
	</select>
	
</mapper>